local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local plr = Players.LocalPlayer
local char = plr.Character or plr.CharacterAdded:Wait()
local hum = char:WaitForChild("Humanoid")
local hrp = char:WaitForChild("HumanoidRootPart")
local cam = workspace.CurrentCamera

---------------------------------------------------------
-- ANIMATE‑STYLE MOVEMENT ANIMATION LOADING (NEW SYSTEM)
---------------------------------------------------------

local animate = char:WaitForChild("Animate")

-- Idle
animate.idle.Animation1.AnimationId = "rbxassetid://130912259300899"
animate.idle.Animation2.AnimationId = "rbxassetid://130912259300899"

-- Walk
animate.walk.WalkAnim.AnimationId = "rbxassetid://102622695004986"

-- Run
animate.run.RunAnim.AnimationId = "rbxassetid://102622695004986"

-- Jump
if animate:FindFirstChild("jump") then
    animate.jump.JumpAnim.AnimationId = "rbxassetid://4211219390"
end

-- Fall
if animate:FindFirstChild("fall") then
    animate.fall.FallAnim.AnimationId = "rbxassetid://656115606"
end

---------------------------------------------------------
-- MANUAL ANIMATIONS (Action4 / special)
---------------------------------------------------------

local animator = hum:WaitForChild("Animator")

local function load(id, priority)
    local a = Instance.new("Animation")
    a.AnimationId = "rbxassetid://" .. id
    local t = animator:LoadAnimation(a)
    t.Priority = priority
    return t
end

local anims = {
    spin   = load(18537367238, Enum.AnimationPriority.Action),
    laugh  = load(127415121971326, Enum.AnimationPriority.Action4),
    appear = load(90669505009197, Enum.AnimationPriority.Action4),
    mark   = load(80372239811417, Enum.AnimationPriority.Action4),
    flight = load(99694231408564, Enum.AnimationPriority.Action)
}

local function stopAction4()
    for _, t in pairs(anims) do
        if t.Priority == Enum.AnimationPriority.Action4 then
            t:Stop()
        end
    end
end

local currentTrack
local function playAnim(name, speed)
    local t = anims[name]
    if not t then return end

    if t.Priority == Enum.AnimationPriority.Action4 then
        stopAction4()
    end

    if currentTrack ~= t then
        if currentTrack then currentTrack:Stop() end
        currentTrack = t
        t:Play()
    end

    if speed then t:AdjustSpeed(speed) end
end

local function stopAll()
    for _, t in pairs(anims) do t:Stop() end
    currentTrack = nil
end

---------------------------------------------------------
-- MOVEMENT SETTINGS
---------------------------------------------------------

local minWalk = 5
local maxWalk = 35 -- updated
local accel = (maxWalk - minWalk) / 5
hum.WalkSpeed = minWalk
hum.JumpHeight = 30

local airborne = false
hum.StateChanged:Connect(function(_, new)
    if new == Enum.HumanoidStateType.Jumping then
        airborne = true
    elseif new == Enum.HumanoidStateType.Landed then
        airborne = false
    end
end)

---------------------------------------------------------
-- SPRINT (SHIFT)
---------------------------------------------------------

local sprinting = false

---------------------------------------------------------
-- SPIN / DASH / FLIGHT
---------------------------------------------------------

local holdingE = false
local spinCharging = false
local chargeStart = 0

local dashActive = false
local dashEndTime = 0
local dashWalkSpeed = 35

local flightMode = false

---------------------------------------------------------
-- FLING
---------------------------------------------------------

local walkflinging = false

local function startWalkFling()
    local Root = hrp
    local Humanoid = hum

    Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
    Humanoid.BreakJointsOnDeath = false

    RunService.Stepped:Connect(function()
        if walkflinging then
            Humanoid.Health = math.huge
            Humanoid.MaxHealth = math.huge
        end
    end)

    walkflinging = true
    Root.CanCollide = false

    task.spawn(function()
        while walkflinging and Root and Root.Parent do
            RunService.Heartbeat:Wait()
            local vel = Root.Velocity
            Root.Velocity = vel * 99999999 + Vector3.new(0, 99999999, 0)
            RunService.RenderStepped:Wait()
            Root.Velocity = vel
            RunService.Stepped:Wait()
            Root.Velocity = vel + Vector3.new(0, 0.1, 0)
        end
    end)
end

local function stopWalkFling()
    walkflinging = false
    if hrp then hrp.CanCollide = true end
end

local function startDash(charge)
    local alpha = math.clamp(charge / 3, 0, 1)
    local duration = 3 + 2 * alpha
    dashEndTime = tick() + duration
    dashActive = true
    startWalkFling()
    playAnim("spin", 3)
end

---------------------------------------------------------
-- TARGETING (R)
---------------------------------------------------------

local mouse = plr:GetMouse()
local targeting = false
local targetData = {}
local hoveredPlayer = nil

local function clearTargets()
    for _, data in pairs(targetData) do
        if data.highlight then data.highlight:Destroy() end
        if data.gui then data.gui:Destroy() end
    end
    targetData = {}
    hoveredPlayer = nil
end

local function enterTargetMode()
    targeting = true
    clearTargets()

    stopAction4()
    playAnim("mark", 1)
    anims.mark.Looped = true

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= plr and p.Character then
            local root = p.Character:FindFirstChild("HumanoidRootPart")
            local head = p.Character:FindFirstChild("Head") or root
            if root and head then
                local h = Instance.new("Highlight")
                h.FillColor = Color3.fromRGB(255, 0, 0)
                h.FillTransparency = 0.5
                h.OutlineColor = Color3.fromRGB(255, 215, 0)
                h.Adornee = p.Character
                h.Parent = p.Character

                local bill = Instance.new("BillboardGui")
                bill.Size = UDim2.new(0, 40, 0, 40)
                bill.AlwaysOnTop = true
                bill.Adornee = head
                bill.Parent = p.Character

                local frame = Instance.new("Frame")
                frame.Size = UDim2.new(1, 0, 1, 0)
                frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
                frame.BackgroundTransparency = 0.5
                frame.Parent = bill

                local corner = Instance.new("UICorner")
                corner.CornerRadius = UDim.new(1, 0)
                corner.Parent = frame

                targetData[p] = {highlight = h, gui = bill, frame = frame}
            end
        end
    end
end

local function exitTargetMode()
    targeting = false
    stopAction4()
    clearTargets()
end

mouse.Move:Connect(function()
    if not targeting then return end
    hoveredPlayer = nil

    local target = mouse.Target
    if target then
        local model = target:FindFirstAncestorOfClass("Model")
        if model then
            local p = Players:GetPlayerFromCharacter(model)
            if p and p ~= plr and targetData[p] then
                hoveredPlayer = p
            end
        end
    end

    for p, data in pairs(targetData) do
        if data.frame then
            data.frame.BackgroundColor3 = (p == hoveredPlayer)
                and Color3.fromRGB(0, 255, 0)
                or  Color3.fromRGB(255, 0, 0)
        end
    end
end)

mouse.Button1Down:Connect(function()
    if not targeting or not hoveredPlayer then return end
    local p = hoveredPlayer
    local ch = p.Character
    if not ch then return end
    local root = ch:FindFirstChild("HumanoidRootPart")
    if not root then return end

    exitTargetMode()

    stopAction4()
    playAnim("laugh", 1)

    task.delay(0.5, function()
        stopAction4()
        hrp.CFrame = root.CFrame + Vector3.new(0, 3, 0)
        playAnim("appear", 1)

        task.delay(1, function()
            stopAction4()
        end)
    end)
end)

---------------------------------------------------------
-- INPUT
---------------------------------------------------------

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end

    if input.KeyCode == Enum.KeyCode.E then
        holdingE = true
        if not airborne then
            spinCharging = true
            chargeStart = tick()
            hum.WalkSpeed = 0
            playAnim("spin", 3)
        else
            flightMode = true
            playAnim("flight", 1)
        end

    elseif input.KeyCode == Enum.KeyCode.Q then
        stopAction4()
        playAnim("laugh", 1)

    elseif input.KeyCode == Enum.KeyCode.R then
        if targeting then exitTargetMode() else enterTargetMode() end
    end
end)

UserInputService.InputEnded:Connect(function(input, gpe)
    if gpe then return end

    if input.KeyCode == Enum.KeyCode.E then
        holdingE = false

        if spinCharging then
            spinCharging = false
            local charge = math.min(tick() - chargeStart, 3)
            startDash(charge)
        end

        if flightMode then
            flightMode = false
            stopAll()
        end
    end
end)

---------------------------------------------------------
-- MAIN LOOP
---------------------------------------------------------

RunService.Heartbeat:Connect(function(dt)
    cam = workspace.CurrentCamera or cam
    local moving = hum.MoveDirection.Magnitude > 0.1
    local state = hum:GetState()

    -- Flight
    if flightMode then
        if cam then
            local dir = cam.CFrame.LookVector
            hrp.AssemblyLinearVelocity = dir * 35
        end
        playAnim("flight", 1)
        return
    end

    -- Dash
    if dashActive then
        if tick() >= dashEndTime then
            dashActive = false
            stopWalkFling()
            hum.WalkSpeed = minWalk
            stopAll()
        else
            hum.WalkSpeed = dashWalkSpeed
            playAnim("spin", 3)
        end
        return
    end

    -- Spin charge
    if spinCharging then
        hum.WalkSpeed = 0
        return
    end

    -- Sprint (Shift)
    sprinting = UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)

    if sprinting then
        hum.WalkSpeed = 50
        playAnim("flight", 5)
        return
    end

    -- Normal run buildup
    if moving then
        hum.WalkSpeed = math.clamp(hum.WalkSpeed + accel * dt, minWalk, maxWalk)
    else
        hum.WalkSpeed = minWalk
    end

    -- Base animations (Animate handles movement)
    if state == Enum.HumanoidStateType.Jumping then
        -- Animate handles jump
    elseif state == Enum.HumanoidStateType.Freefall then
        -- Animate handles fall
    end
end)
